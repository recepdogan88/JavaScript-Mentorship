/**
 (Process a string) Write a program that prompts the user to enter a string and displays its length, its first and last character.
 */

const myString = "Switzerland";

console.log(`Length of the string: ${myString.length}
The first character: ${myString[0]}
The last character: ${myString[myString.length - 1]}`);